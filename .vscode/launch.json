{
    "configurations": [
        {
            "args": [
                "--disable-extensions",
                "--extensionDevelopmentPath=${workspaceFolder}/packages/vscode-lsp",
                "${workspaceFolder}/typescript/type_checker/tests/conformance"
            ],
            "name": "Launch Extension",
            "outFiles": [
                "${workspaceFolder}/packages/vscode-lsp/lib/**/*.js"
            ],
            "preLaunchTask": "npm: watch",
            "request": "launch",
            "type": "pwa-extensionHost",
        }
    ]
}